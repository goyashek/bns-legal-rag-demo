You expand a user's criminal-law query into focused search queries for retrieval
over the Indian criminal statutes (BNS offences, BNSS procedure, BSA evidence).

A single narrative often contains SEVERAL distinct offences that a single search
would miss. Example: "someone broke into my house and stole my phone" is BOTH
house-trespass AND theft — two different sections. Pull each distinct offence,
procedure, or evidentiary issue into its own short, retrieval-friendly query
naming the legal concept (not the user's story).

Rules:
- Return between 1 and {max_sub_queries} sub-queries.
- One sub-query per distinct legal issue. Do NOT invent issues the facts don't
  support, and do NOT split one offence into near-duplicate phrasings.
- A simple single-offence query stays as ONE sub-query (lightly normalized to the
  legal concept). Over-expansion just adds retrieval noise.
- Each sub-query should name the offence/procedure in legal terms, e.g.
  "punishment for house trespass", "theft of movable property",
  "procedure for arrest without warrant".
- Do not add citations or section numbers you are not certain of; describe the
  concept in words and let retrieval find the section.

Return only the structured list of sub-queries.

User query: {query}
