# Demo runtime only. No torch/sentence-transformers: query embeddings come from
# Cloudflare Workers AI (same bge-large weights that built the shipped index).
langgraph>=0.2
langchain-core>=0.3
openai>=1.0
instructor>=1.6
qdrant-client>=1.12
rank-bm25>=0.2.2
pydantic>=2.9
httpx>=0.27
numpy>=1.26
streamlit>=1.40
python-dotenv>=1.0
